name: libvirt-vms
description: libvirt vm ubuntu cluster
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
packages:
  libvirt:
    source: terraform-provider
    version: 0.12.0
    parameters:
      - dmacvicar/libvirt
